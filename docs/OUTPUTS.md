# 输出与可清理文件说明

本项目运行测试与示例后会生成以下输出目录或文件。若不再需要，可手动删除以节省空间。

## 运行输出目录

- `outputs/mission1/`：任务1流程输出（`.mat` + 图像）
- `outputs/mission2/`：任务2流程输出（`.mat` + 图像）
- `outputs/test_output/`：导出功能测试输出（可选）

## 临时文件

- `outputs/temp_roundtrip_test.mat`：往返测试临时文件

## 说明

删除上述文件/目录不会影响核心代码与功能，仅影响示例/测试结果。
